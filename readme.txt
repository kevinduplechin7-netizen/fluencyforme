FluentHour (static build)

Run (PowerShell):
- npm install
- npm run dev

Then open:
- http://localhost:5173/

This build includes two on-screen helper cards on the session screen:
- Send to FluentHour Companion
- Send to Fluent Hour Phase Actor

Both provide buttons:
- Copy this phase
- Copy whole session
- Copy + Open
