[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback (keeps your in-app routing working)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
